import { test, expect } from '@playwright/test';
import { LoginPage } from '../pages/login.page';

test('TC_09_Verify_New_User_Can_Register_Successfully', async ({ page }) => {
    const loginPage = new LoginPage(page);

    // Mở tab (page) mới từ demo test architec
    const tab1 = await page.newPage();
    await tab1.goto('https://www.guerrillamail.com/');

    //Wait 5 minutes
    // "Your TestArchitect Sample Website account has been created" 
    // Email //tbody[@id="email_list"]//td[contains(normalize-space(.), 'Your TestArchitect Sample Website account has been created')]

});